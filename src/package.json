{
  "name": "subway-surfers-api-web",
  "version": "1.0.0",
  "description": "Subway Surfers API Web Interface",
  "type": "module",
  "scripts": {
    "dev": "echo 'This is a static HTML project. Open index.html in a browser or use a local server.'",
    "build": "echo 'No build step required for static HTML project'",
    "lint": "echo 'No linter configured'"
  },
  "dependencies": {
    "@vercel/analytics": "^1.4.1"
  }
}
